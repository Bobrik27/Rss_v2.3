---
interface Props {
  data: Array<{
    title: string;
    icon: any;
    description: string;
  }>;
  title: string;
  sub: string;
}
const { data, title, sub } = Astro.props;
---

<section class="relative">
  <div class="container mx-auto px-4">
    <!-- Section header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <h2 class="text-4xl md:text-5xl font-black tracking-tight text-white mb-4">
        {title}
      </h2>
      <p class="text-lg md:text-xl text-text-muted">
        {sub}
      </p>
    </div>

    <!-- Cards grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {data.map((item, idx) => {
        const Icon = item.icon;
        return (
          <div
            class="group bg-card border border-white/5 rounded-2xl p-6 hover:border-primary/50 transition-all duration-300 hover:shadow-2xl hover:shadow-primary/10"
          >
            <div class="flex items-start gap-4">
              <div class="p-3 rounded-xl bg-primary/10 text-primary group-hover:bg-primary/20 transition-colors">
                <Icon class="w-6 h-6" />
              </div>
              <div class="flex-1">
                <h3 class="text-xl font-bold text-white mb-2">
                  {item.title}
                </h3>
                <p class="text-text-muted text-sm leading-relaxed">
                  {item.description}
                </p>
              </div>
            </div>
          </div>
        );
      })}
    </div>
  </div>
</section>