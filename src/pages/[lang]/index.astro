---
import { getDictionary } from '../../i18n/ui.ts';
import Layout from '../../layouts/Layout.astro';
import StaticHero from '../../components/StaticHero.astro';

export async function getStaticPaths() {
  return [
    { params: { lang: 'ru' } },
    { params: { lang: 'en' } },
    { params: { lang: 'de' } },
  ];
}

const { lang } = Astro.params;
const dictionary = getDictionary(lang as 'ru' | 'en' | 'de');
---

<Layout title={`${dictionary.hero.title} | Run Swift Studio v3`} lang={lang}>
  <StaticHero />

  <main class="container mx-auto px-4 py-12 md:py-24">
    <section class="max-w-4xl mx-auto text-center">
      <h2 class="text-4xl md:text-5xl font-bold tracking-tight mb-6">
        {dictionary.hero.title}
      </h2>
      <p class="text-xl md:text-2xl text-text-muted mb-10 max-w-3xl mx-auto">
        {dictionary.hero.subtitle}
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href={`/${lang}/contact`}
          class="inline-flex items-center justify-center px-8 py-3 text-lg font-semibold rounded-lg bg-primary text-white hover:bg-primary-hover transition-colors duration-200"
        >
          {dictionary.hero.cta}
        </a>
        <a
          href={`/${lang}/portfolio`}
          class="inline-flex items-center justify-center px-8 py-3 text-lg font-semibold rounded-lg border-2 border-border text-text hover:border-primary transition-colors duration-200"
        >
          {dictionary.nav.portfolio}
        </a>
      </div>
    </section>

    <section class="mt-32 grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="bg-surface p-8 rounded-2xl">
        <h3 class="text-2xl font-bold mb-4">Скорость</h3>
        <p class="text-text-muted">
          Мы разрабатываем продукты, которые загружаются мгновенно и работают без задержек.
        </p>
      </div>
      <div class="bg-surface p-8 rounded-2xl">
        <h3 class="text-2xl font-bold mb-4">Качество</h3>
        <p class="text-text-muted">
          Каждая деталь проходит тщательную проверку, чтобы результат превзошёл ожидания.
        </p>
      </div>
      <div class="bg-surface p-8 rounded-2xl">
        <h3 class="text-2xl font-bold mb-4">Подход</h3>
        <p class="text-text-muted">
          Используем современные технологии и методологии для достижения лучших результатов.
        </p>
      </div>
    </section>
  </main>
</Layout>